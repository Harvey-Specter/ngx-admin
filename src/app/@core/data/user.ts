//import { UUID } from 'angular2-uuid';

export class User {
  //id = UUID.UUID();
  email : string;
  passwd : string;
  changepasswd: string;
  userdesc : string;
  userrole : string;
  displayname : string;
  privileges: any;
  status:string;
}
