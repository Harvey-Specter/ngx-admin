//import { UUID } from 'angular2-uuid';

export class Peer {
  //id = UUID.UUID;
  peer_fullname:string;
  peer_hostname_port: string;
  org_fullname : string;
  org_name:string;
  //array1:Array<number>;
}
